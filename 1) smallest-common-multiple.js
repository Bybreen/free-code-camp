function smallestCommons(arr) {
  var range =arr.sort();
  //sort the array
  var array = [""];
  // create empty array
  for (var i= arr[0]; i<= arr[arr.length-1]; i++){
    array.push(i);
    // adds to end of array
  }
  var x = true;
  return arr;
}


smallestCommons([5,1]);
